// version.js — the game's version, shown in Settings and on the Wiki's "What's new"
// page (wiki/guides/whats-new.md reads it through {{cfg:version.current}}).
// Keep package.json's "version" in step (npm run validate checks it).
export const GAME_VERSION = '0.10.0';
